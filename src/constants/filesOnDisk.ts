import { IFile } from "../contracts/interface/IFile";
import { IFolder } from "../contracts/interface/IFolder";

export const files: Array<IFile | IFolder> = [
    {
        name: 'root',
        contents: []
    }
]